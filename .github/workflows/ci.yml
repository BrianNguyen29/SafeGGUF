name: CI

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

jobs:
  core:
    name: Core tests on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-14]
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.13.0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Verify Code Formatting
        run: zig fmt --check src/ build.zig tests/*.zig

      - name: Generate Fixtures and Seed Corpus
        run: python tests/generate_fixtures.py

      - name: Run Exhaustive Unit, Fuzz, & Regression Tests
        run: zig build test --summary all

      - name: Build Binary (ReleaseSafe)
        run: zig build -Doptimize=ReleaseSafe

      - name: Run End-to-End CLI Integration Tests (Exact Exit Codes & Invariants)
        run: python tests/cli_test.py

      - name: Run Negative Corpus & Advisory Provenance Checks (Exit 2 Reject Contract)
        run: python tests/negative_corpus.py

      - name: Verify C-ABI Compilation & Test Python Bindings
        run: python bindings/python/tests/test_binding.py

      - name: Run Hybrid Triage Test Suite
        run: python tests/test_triage.py

      - name: Run FFI Boundary Fuzzing Suite
        run: python tests/fuzz_ffi_boundaries.py

  # Version metadata guard: `build.zig` embeds the default version that source
  # builds report (`safegguf --version`). It must track the latest release tag,
  # or a plain `zig build` from `main` claims an older release than the one
  # published. Needs a full-history checkout so `git tag` sees every release.
  version-consistency:
    name: Embedded Version vs Latest Tag
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout repository (full history, all tags)
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Verify Embedded Default Version Tracks the Latest Release Tag
        run: python scripts/version_consistency.py

  # Tier-1 real-corpus gate (F-04/B1): evaluates manifest-selected entries
  # against the ReleaseSafe binary and enforces the runner's coverage floors (a
  # run with 0 verified entries can never PASS).
  #
  # PR lane (network-neutral): --tolerate-download-errors plus the runner's
  # NEUTRAL exit 3 (report status inconclusive) keep a network/DNS outage from
  # reddening a PR, while real findings (size/hash mismatch, verdict mismatch,
  # CLI error) and unmet coverage floors still fail the job.
  #
  # Release lane (fail-closed): on v* tags the same gate runs without
  # --tolerate-download-errors and the release job depends on it, so an outage,
  # a skipped entry or an unmet floor always blocks the release.
  # The nightly advisory run with cache + report artifact stays in
  # .github/workflows/real-corpus.yml.
  real-corpus:
    name: Real-corpus gate (tier 1) on ubuntu-24.04
    if: github.event_name == 'pull_request' || startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.13.0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Build Binary (ReleaseSafe)
        run: zig build -Doptimize=ReleaseSafe

      - name: Run Real-Corpus Gate (PR network-neutral; v* tag fail-closed)
        run: |
          rc=0
          if [ "$GITHUB_EVENT_NAME" = "pull_request" ]; then
            python tests/real_corpus.py --tier 1 --tolerate-download-errors \
              --report .cache/real-corpus/real-corpus-report.json || rc=$?
            if [ "$rc" -eq 3 ]; then
              echo "::notice::real-corpus gate inconclusive (network-only failure); neutral on PR"
              rc=0
            fi
          else
            python tests/real_corpus.py --tier 1 \
              --report .cache/real-corpus/real-corpus-report.json || rc=$?
          fi
          exit "$rc"

  oracle:
    name: "Oracle & differential on ${{ matrix.os }}"
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-14]
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.13.0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Generate Fixtures and Seed Corpus
        run: python tests/generate_fixtures.py

      - name: Build Binary (ReleaseSafe)
        run: zig build -Doptimize=ReleaseSafe

      - name: Build Upstream ggml 0.23.0 Oracle
        run: bash tests/build_oracle.sh

      - name: Run Independent Arithmetic Oracle Verification (Python Bigint)
        run: python tests/arithmetic_oracle.py

      - name: Run Independent Type Oracle Verification
        run: python tests/test_oracle_types.py

      - name: Generate Differential Matrix Fixtures
        run: python tests/differential_matrix.py

      - name: Run True Differential Test Suite (SafeGGUF vs Upstream ggml 0.23.0)
        run: python tests/differential.py

  fuzz:
    name: Fuzz on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-14]
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.13.0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Generate Fixtures and Seed Corpus
        run: python tests/generate_fixtures.py

      - name: Build Binary (ReleaseSafe)
        run: zig build -Doptimize=ReleaseSafe

      - name: Verify Fuzz Crash Handler Persistence
        run: python tests/fuzz_mutation.py --test-crash-handler

      - name: Run High-Throughput Mutation Fuzzing (2,000 Iterations, 4,000 Executions)
        run: python tests/fuzz_mutation.py --iterations 2000

      - name: Upload Fuzz Crash Artifacts on Failure
        if: failure() || cancelled()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: fuzz-artifacts-${{ matrix.os }}
          path: tests/fuzz-artifacts/
          if-no-files-found: ignore

  bench:
    name: Bench on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-24.04, macos-14]
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.13.0

      - name: Run Resource-Budget Benchmark Suite (Scales + Instrumented Backend-Read Gate)
        run: zig build bench --summary all

  # Fail-closed Windows release gate: calls the Windows runtime lane
  # (windows.yml) as a reusable workflow, so the release job can depend on the
  # exact native-Windows validation that PRs and main pushes run without
  # duplicating a single step. windows.yml intentionally drops its own tag
  # trigger, so a v* tag runs that lane exactly once - here, against the tagged
  # commit. The gate is tag-only, matching release's trigger.
  windows-gate:
    name: Windows runtime gate (reusable windows.yml)
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: read
    uses: ./.github/workflows/windows.yml

  release:
    name: Build & Publish Release Artifacts
    # real-corpus runs fail-closed on v* tags (no --tolerate-download-errors):
    # the gate must PASS with coverage floors met, so an outage, a skipped
    # entry or an unmet floor blocks the release.
    # windows-gate is equally fail-closed: a failed or skipped Windows runtime
    # gate skips this job, so safegguf-x86_64-windows.exe is never published
    # without native validation of the tagged commit.
    needs: [core, version-consistency, oracle, fuzz, bench, real-corpus, windows-gate]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29 # v2.2.1
        with:
          version: 0.13.0

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'

      - name: Build Cross-Platform Release Binaries & SHA256 Manifest
        run: |
          mkdir -p dist
          # Tag-derived: release artifacts must report the tag they were built
          # from, not the build.zig embedded default that `version-consistency`
          # guards for source builds.
          version="${GITHUB_REF_NAME#v}"
          targets=("x86_64-linux" "aarch64-linux" "x86_64-macos" "aarch64-macos" "x86_64-windows")
          for target in "${targets[@]}"; do
            echo "Building for $target..."
            ext=""
            if [[ "$target" == *"windows"* ]]; then
              ext=".exe"
            fi
            zig build -Doptimize=ReleaseSafe -Dtarget=$target -Dversion="$version"
            cp zig-out/bin/safegguf${ext} dist/safegguf-${target}${ext}
          done
          cd dist
          sha256sum safegguf-* > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - name: Generate SPDX 2.3 SBOM (dist/safegguf.spdx.json)
        run: |
          python3 - <<'PY'
          import datetime
          import hashlib
          import json
          import os
          import subprocess

          repo = os.environ["GITHUB_REPOSITORY"]
          server = os.environ.get("GITHUB_SERVER_URL", "https://github.com")
          tag = os.environ["GITHUB_REF_NAME"]
          version = tag[1:] if tag.startswith("v") else tag
          commit = os.environ["GITHUB_SHA"]
          run_id = os.environ["GITHUB_RUN_ID"]
          zig_version = subprocess.run(
              ["zig", "version"], capture_output=True, text=True, check=True
          ).stdout.strip()

          def checksum(algorithm, path):
              digest = hashlib.new(algorithm)
              with open(path, "rb") as f:
                  for chunk in iter(lambda: f.read(1 << 20), b""):
                      digest.update(chunk)
              return digest.hexdigest()

          def spdx_id(name):
              return "SPDXRef-File-" + name.replace(".", "-").replace("_", "-")

          files = []
          relationships = []
          for name in sorted(os.listdir("dist")):
              if name == "safegguf.spdx.json":
                  continue
              path = os.path.join("dist", name)
              files.append({
                  "fileName": name,
                  "SPDXID": spdx_id(name),
                  "checksums": [
                      {"algorithm": "SHA1", "checksumValue": checksum("sha1", path)},
                      {"algorithm": "SHA256", "checksumValue": checksum("sha256", path)},
                  ],
                  "licenseConcluded": "NOASSERTION",
                  "copyrightText": "NOASSERTION",
              })
              relationships.append({
                  "spdxElementId": "SPDXRef-Package-safegguf",
                  "relatedSpdxElement": spdx_id(name),
                  "relationshipType": "CONTAINS",
              })

          document = {
              "spdxVersion": "SPDX-2.3",
              "dataLicense": "CC0-1.0",
              "SPDXID": "SPDXRef-DOCUMENT",
              "name": "safegguf-" + version,
              "documentNamespace": f"{server}/{repo}/releases/download/{tag}/safegguf-{version}.spdx.json",
              "creationInfo": {
                  "created": datetime.datetime.now(datetime.timezone.utc).strftime("%Y-%m-%dT%H:%M:%SZ"),
                  "creators": ["Tool: safegguf-release-sbom", "Organization: SafeGGUF"],
                  "comment": f"Generated by GitHub Actions run {run_id} for {tag} ({commit})",
              },
              "packages": [{
                  "name": "safegguf",
                  "SPDXID": "SPDXRef-Package-safegguf",
                  "versionInfo": version,
                  "downloadLocation": f"{server}/{repo}",
                  "filesAnalyzed": True,
                  "licenseConcluded": "MIT",
                  "licenseDeclared": "MIT",
                  "copyrightText": "NOASSERTION",
                  "sourceInfo": f"Built from {server}/{repo}@{commit} with Zig {zig_version} (ReleaseSafe) by GitHub Actions run {run_id}",
                  "externalRefs": [{
                      "referenceCategory": "PACKAGE-MANAGER",
                      "referenceType": "purl",
                      "referenceLocator": f"pkg:github/{repo}@{commit}",
                  }],
              }],
              "files": files,
              "relationships": [{
                  "spdxElementId": "SPDXRef-DOCUMENT",
                  "relatedSpdxElement": "SPDXRef-Package-safegguf",
                  "relationshipType": "DESCRIBES",
              }] + relationships,
          }

          with open("dist/safegguf.spdx.json", "w", encoding="utf-8") as f:
              json.dump(document, f, indent=2, sort_keys=True)
              f.write("\n")
          print("Wrote dist/safegguf.spdx.json")
          PY

      - name: Attest Build Provenance for Release Binaries
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: 'dist/safegguf-*'

      - name: Attest SPDX SBOM
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: 'dist/safegguf-*'
          sbom-path: 'dist/safegguf.spdx.json'

      - name: Install Cosign
        uses: sigstore/cosign-installer@7e8b541eb2e61bf99390e1afd4be13a184e9ebc5 # v3.10.1
        with:
          cosign-release: 'v2.6.5'

      - name: Keyless Sign SHA256 Manifest (Sigstore)
        working-directory: dist
        run: cosign sign-blob --yes --bundle SHA256SUMS.txt.sigstore.json SHA256SUMS.txt

      - name: Verify Release Artifacts Before Publish
        working-directory: dist
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "Verifying SHA256 manifest..."
          sha256sum -c SHA256SUMS.txt
          echo "Verifying keyless signature on SHA256SUMS.txt..."
          cosign verify-blob \
            --bundle SHA256SUMS.txt.sigstore.json \
            --certificate-identity-regexp "^https://github.com/${GITHUB_REPOSITORY}/\.github/workflows/ci\.yml@refs/tags/.*$" \
            --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
            SHA256SUMS.txt
          echo "Verifying artifact provenance attestations..."
          for artifact in safegguf-*; do
            gh attestation verify "$artifact" --repo "$GITHUB_REPOSITORY"
          done
          echo "All pre-publish verifications passed."

      - name: Upload Binaries, Checksums, SBOM & Signatures to GitHub Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag_name="${GITHUB_REF#refs/tags/}"
          echo "Ensuring release $tag_name exists..."
          gh release view "$tag_name" >/dev/null 2>&1 || gh release create "$tag_name" --title "$tag_name" --generate-notes
          echo "Uploading assets to release $tag_name..."
          gh release upload "$tag_name" dist/* --clobber
